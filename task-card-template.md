# 任务卡：<一句话目标>

> 一张卡 = 一个 agent 从头跑到尾（目标 1–3 小时），中途不找人。
> 写不出「验收命令」的活不要派，先拆小或先派一张调研卡。

## 目标
<要达成的可观察结果，一两句>

## 范围
- 可改：<目录 / 文件 glob>
- 只读参考：<目录>
- 禁止：协议、DB schema、鉴权、反作弊、生产配置；lockfile 和 CI 配置（除非本卡就是改它们）

## 验收（必须机器可判，全部退出码为 0 才算完成）
```bash
<build / test / lint 命令>
<本卡专属断言：新增的测试名、脚本输出比对等>
```

## 完成定义
1. 验收命令在本分支 HEAD 上全绿，原始输出贴进 PR 描述
2. PR 已开；描述里写清：改了什么、为什么、做了哪些假设、哪些没做
3. 审查 P0 = P1 = 0（按解析出的数量判定，不看 VERDICT 字面）；P2/P3 记进 backlog issue，不在本卡里修
4. 风险 = 低 → 允许自动合并；中 / 高 → 停在 PR 等人

## 自主规则
- 有歧义就按工程判断选一个，写进 PR 的「假设」段，继续做，不停下来问
- 只在这些情况停：需要凭证 / 付费 / 生产访问；需要碰「禁止」范围；验收命令本身有问题。
  停的时候写 `BLOCKED.md`（卡在哪、需要谁决定什么、已经试过什么），然后退出
- 等 CI 超过 10 分钟：推完就退出，由派单器在 CI 出结果后续跑，不占并发名额

## 预算
- 时长上限：<3h>　模型：<sonnet | opus | fable>　（走 API 计费时加 `--max-budget-usd <N>`）

## 元数据
- 风险：低 | 中 | 高　依赖：<必须先合并的卡>　验收人：<你>
